#!/bin/zsh
echo "🔍 Running local complexity and build checks..."

# Run Maven verify phase
mvn clean compile verify

# Check if Maven failed
if [ $? -ne 0 ]; then
    echo "❌ Error: Code complexity or build checks failed. Commit aborted."
    exit 1
fi

echo "✅ All checks passed! Proceeding with commit..."
exit 0